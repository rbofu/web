{
  "name": "ntdcp-website",
  "version": "1.0.0",
  "private": true,
  "description": "NTDCP website + Programme Calendar (Express backend serves the static frontend and the /calendar app).",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
