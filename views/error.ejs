<%- include('partials/header', { title: title, active: '', currentUser: (typeof currentUser !== 'undefined' ? currentUser : null) }) %>
<div class="empty-state">
  <h2><%= title %></h2>
  <p><%= message %></p>
  <a href="/" class="btn-primary">Back to calendar</a>
</div>
<%- include('partials/footer') %>
